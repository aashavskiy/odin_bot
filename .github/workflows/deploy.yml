name: Deploy to Cloud Run

on:
  push:
    branches:
      - main

env:
  SERVICE_NAME: odin-bot
  REGION: ${{ vars.GCP_REGION || secrets.GCP_REGION }}
  PROJECT_ID: ${{ vars.GCP_PROJECT_ID || secrets.GCP_PROJECT_ID }}
  WEBHOOK_BASE: ${{ vars.WEBHOOK_BASE || secrets.WEBHOOK_BASE }}
  BOT_TOKEN: ${{ vars.BOT_TOKEN || secrets.BOT_TOKEN }}
  OPENAI_API_KEY: ${{ vars.OPENAI_API_KEY || secrets.OPENAI_API_KEY }}
  OPENAI_FAST_MODEL: ${{ vars.OPENAI_FAST_MODEL || secrets.OPENAI_FAST_MODEL || 'gpt-4.1-mini' }}
  ADMIN_ID: ${{ vars.ADMIN_ID || secrets.ADMIN_ID }}
  TASKS_LOCATION: ${{ vars.TASKS_LOCATION || secrets.TASKS_LOCATION }}
  TASKS_QUEUE: ${{ vars.TASKS_QUEUE || secrets.TASKS_QUEUE }}
  TASKS_BASE: ${{ vars.TASKS_BASE || secrets.TASKS_BASE }}
  TASKS_TOKEN: ${{ vars.TASKS_TOKEN || secrets.TASKS_TOKEN }}
  REMINDER_CONFIDENCE_THRESHOLD: ${{ vars.REMINDER_CONFIDENCE_THRESHOLD || secrets.REMINDER_CONFIDENCE_THRESHOLD || '0.7' }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Auth to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure default region
        run: |
          if [ -z "${REGION}" ]; then
            echo "GCP_REGION is not set. Define repo variable GCP_REGION (e.g. us-central1)." >&2
            exit 1
          fi
          if [ -z "${PROJECT_ID}" ]; then
            echo "GCP_PROJECT_ID is not set. Define repo variable GCP_PROJECT_ID." >&2
            exit 1
          fi
          gcloud config set project "${PROJECT_ID}"
          gcloud config set run/region "${REGION}"

      - name: Validate secrets
        run: |
          if [ -z "${BOT_TOKEN}" ]; then
            echo "BOT_TOKEN is not set (vars or secrets)." >&2
            exit 1
          fi
          if [ -z "${OPENAI_API_KEY}" ]; then
            echo "OPENAI_API_KEY is not set (vars or secrets)." >&2
            exit 1
          fi
          if [ -z "${ADMIN_ID}" ]; then
            echo "ADMIN_ID is not set (vars or secrets)." >&2
            exit 1
          fi

      - name: Configure Docker auth
        run: gcloud auth configure-docker --quiet

      - name: Build and push image
        run: |
          IMAGE="gcr.io/${PROJECT_ID}/${SERVICE_NAME}"
          BUILD_ID="$(gcloud builds submit --async --tag "${IMAGE}" --format="value(id)")"
          if [ -z "${BUILD_ID}" ]; then
            echo "Cloud Build did not return a build ID." >&2
            exit 1
          fi
          for i in {1..120}; do
            STATUS="$(gcloud builds describe "${BUILD_ID}" --format="value(status)")"
            echo "Cloud Build status: ${STATUS}"
            if [ "${STATUS}" = "SUCCESS" ]; then
              exit 0
            fi
            if [ "${STATUS}" = "FAILURE" ] || [ "${STATUS}" = "CANCELLED" ] || [ "${STATUS}" = "TIMEOUT" ]; then
              exit 1
            fi
            sleep 5
          done
          echo "Timed out waiting for Cloud Build to complete." >&2
          exit 1

      - name: Deploy to Cloud Run
        run: |
          IMAGE="gcr.io/${PROJECT_ID}/${SERVICE_NAME}"
          OPENAI_FAST_MODEL="${OPENAI_FAST_MODEL:-gpt-4.1-mini}"
          ENV_VARS="BOT_TOKEN=${BOT_TOKEN},OPENAI_API_KEY=${OPENAI_API_KEY},GCP_PROJECT_ID=${PROJECT_ID},ADMIN_ID=${ADMIN_ID},WEBHOOK_PATH=/webhook,HISTORY_MAX_MESSAGES=16,SUMMARY_TRIGGER=20,HISTORY_TTL_DAYS=7,FIRESTORE_DISABLED=0,OPENAI_FAST_MODEL=${OPENAI_FAST_MODEL},REMINDER_CONFIDENCE_THRESHOLD=${REMINDER_CONFIDENCE_THRESHOLD}"
          if [ -n "${WEBHOOK_BASE}" ]; then
            ENV_VARS="${ENV_VARS},WEBHOOK_BASE=${WEBHOOK_BASE}"
          fi
          if [ -n "${TASKS_LOCATION}" ]; then
            ENV_VARS="${ENV_VARS},TASKS_LOCATION=${TASKS_LOCATION}"
          fi
          if [ -n "${TASKS_QUEUE}" ]; then
            ENV_VARS="${ENV_VARS},TASKS_QUEUE=${TASKS_QUEUE}"
          fi
          if [ -n "${TASKS_BASE}" ]; then
            ENV_VARS="${ENV_VARS},TASKS_BASE=${TASKS_BASE}"
          fi
          if [ -n "${TASKS_TOKEN}" ]; then
            ENV_VARS="${ENV_VARS},TASKS_TOKEN=${TASKS_TOKEN}"
          fi
          gcloud run deploy "${SERVICE_NAME}" \
            --image "${IMAGE}" \
            --region "${REGION}" \
            --platform managed \
            --allow-unauthenticated \
            --set-env-vars "${ENV_VARS}"
